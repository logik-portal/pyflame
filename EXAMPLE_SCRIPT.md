# Example Script

When using this library do not include the _example folder. This contains an example script/folder structure using the pyflame library.

Including this folder with a script could cause python errors in Flame.
